/* Confirm that user wants to exit the current webpage. */
function confirmExit() {
  return confirm("This link goes to a non-USG website. This site may not have USG standard privacy control. Click OK to proceed in a separate browser window");
}
